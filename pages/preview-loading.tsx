import WalletCheckingScreen from '../components/TimeLoading';

/**
 * Preview route: shows only WalletCheckingScreen.
 * Open http://localhost:3000/preview-loading to see the loading screen in isolation.
 */
export default function PreviewLoading() {
  return <WalletCheckingScreen />;
}
